{
  "name": "stratos-api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "AppSync Lambda resolvers for Stratos — app-layer authz + the RLS claim bridge.",
  "scripts": {
    "test": "node --test",
    "build": "node build.mjs"
  },
  "dependencies": {
    "@aws-sdk/client-bedrock-runtime": "^3.1079.0",
    "pg": "^8.13.1"
  },
  "devDependencies": {
    "@electric-sql/pglite": "^0.2.17",
    "esbuild": "^0.24.0"
  }
}
